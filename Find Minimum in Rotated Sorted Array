class Solution {
public:
    int findMin(vector<int>& nums) {
        int min = 888;
        for( int i = 0; i < nums.size(); i++){
            if(nums[i] < min){
                min = nums[i];
            }
        }
        return min;
    }
};
